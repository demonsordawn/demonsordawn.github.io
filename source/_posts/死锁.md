--- 
title: '死锁'
tags: ['算法']
---
### 产生死锁的四个必要条件
#### 
1. 互斥:至少有一个资源必须处于非共享模式,即一次只有一个进程可以使用该资源,如果另一个进程请求该资源,那么请求进程必须等待,直到该进程被释放
2. 占有并等待:一个进程必须持有至少一个资源,并同时等待获取另一个被其他进程所持有的资源
3. 不可抢占:资源不能强制的从持有它的进程中抢占,只有在该资源的进程完成后,主动释放它
4. 循环等待:存在一个进程集合{P1,P2,...,Pn},其中P1正在等待P2占有的资源,p2正在等待P3占有的资源,...,Pn正在等待P1占有的资源,形成了一个进程之间的循环等待链
